// import { PAGE_LIST } from '../../types/pageList';

// Feature('"Example" page');

// Before(({ I }) => {
//   I.amOnPage('/');
//   I.amReallyOnPage(PAGE_LIST.EXAMPLE_PAGE);
// });

// Scenario('"Main" page displays correctly', async ({ I }) => {
//   await I.checkPageContent(PAGE_LIST.EXAMPLE_PAGE);
// });
